document.addEventListener("DOMContentLoaded", function () {
  // Add a listener for the "addRecipeForm" form
  document.getElementById("addRecipeForm").addEventListener("submit", function () {
    setTimeout(function () {
      document.getElementById("addRecipeForm").reset();
    }, 100);
  });

  // Add a listener for the "recipeSearchForm" form
  document.getElementById("recipeSearchForm").addEventListener("submit", function () {
    setTimeout(function () {
      document.getElementById("recipeSearchForm").reset();
    }, 100);
  });

  // Add a listener for the "recipeSearchForm" form
  document.getElementById("recipeSearchForm").addEventListener("submit", function () {
    setTimeout(function () {
      document.getElementById("recipeSearchForm").reset();
    }, 100);
  });
});
